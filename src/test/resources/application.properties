spring.profiles.active=test

# Connection url/username/password are injected at runtime by Testcontainers
# (see TicketTrackingSystemApplicationTests @DynamicPropertySource)
spring.datasource.driver-class-name=org.postgresql.Driver
